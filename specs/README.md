# specs/

本目录由 Spec Kit Feature 工作流管理。

规则：

- 不手工创建“看起来像 Spec Kit 生成”的 Feature 目录来绕过命令。
- Feature 应通过 `$speckit-specify` 创建。
- 每个 Feature 的 spec / plan / tasks / checklist 属于开发事实来源。
- 架构级长期决策同时写入 `docs/adr/`。
- 发现代码与 Spec 漂移时使用 converge 流程处理。
